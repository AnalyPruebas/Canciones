<%- include('header',{titulo:'Crear Canción'}); %>

<div class="container">
    <div class="row">
        <div class="col-sm-6">
            <% const estaCreando = typeof cancion === "undefined"%>


            <h1> <%= estaCreando ? 'Crear':'Actualizar' %> cancion</h1>

            <p><%= estaCreando ? 'Cree':'Actualice' %> una cancion</p>

            <!-- emmet -->
            <form action="/cancion/<%= estaCreando ? 'crear' : 'actualizar' %>-cancion/<%= estaCreando ? '' : noticia.id %>"
                  method="POST">

                <div>
                    <label for="nombre">Nombre</label>
                    <input id="nombre"
                           name="nombre"
                           type="text"
                           value="<%= estaCreando ? '' : cancion.nombre %>"
                    >
                </div>
                <div>
                    <label for="anio">Año</label>
                    <input id="anio"
                           name="anio"
                           type="text"
                           value="<%= estaCreando? '': cancion.anio%>"
                    >
                </div>

                <button><%= estaCreando ? 'Crear' : 'Actualizar' %> cancion</button>

            </form>

            <a href="/cancion/inicio">
                <button>Regresar</button>
            </a>
        </div>

    </div>
</div>


<%- include('librerias')%>

<%- include('footer')%>